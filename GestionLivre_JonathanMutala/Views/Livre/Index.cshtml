@model IEnumerable<GestionLivre_JonathanMutala.Models.LivreModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TitreLivre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuteurLivre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatePublicationLivre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TitreLivre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AuteurLivre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatePublicationLivre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDLivre">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDLivre">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDLivre">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
